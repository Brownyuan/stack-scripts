
# docker swatm join, replace `TOKEN` `IP` below
# ssh to MANAGER  and run "docker swarm join-token worker" to get token, IP
# e.g,
# ssh dorry@<managerIP> docker swarm join-token worker
TOKEN="MYTOKEN"
IP="MYIP"
PORT="2377"

